.DS_Store
node_modules/
config.local.js
*.log

# Phase 3 local review pulls (per reviewed site)
reviews/*/feedback.json
reviews/*/photos/

# Supabase secret key for sync.mjs auto-discovery — NEVER commit
reviews/secret.local.json
.env

# Tower: machine-written session state (folder stays tracked for curated TASKS.md)
tower/status.json
tower/.status.*.tmp
